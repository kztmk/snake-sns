{
  "rules": {
    "public": {
      "info": {
        ".read": true,
        ".write": true
      }
    },
    "user-data": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid"
      }
    }
  }
}
